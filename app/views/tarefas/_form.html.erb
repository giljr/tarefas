<%= form_with(model: tarefa) do |form| %>
  <% if tarefa.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(tarefa.errors.count, "error") %> prohibited this tarefa from being saved:</h2>

      <ul>
        <% tarefa.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nota, style: "display: block" %>
    <%= form.textarea :nota %>
  </div>

  <div>
    <%= form.label :feito, style: "display: block" %>
    <%= form.checkbox :feito %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
